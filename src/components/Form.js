import React from 'react';

const Form = () => (
  <div>
    <h2>ADD NEW BOOK</h2>
    <div>
      <input placeholder="Book title" />
      <input placeholder="Book author" />
      <button type="submit" label="Add">ADD BOOK</button>
    </div>
  </div>
);

export default Form;
